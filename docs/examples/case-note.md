# Case Note: Suspicious login followed by MFA fatigue

_Synthetic case note generated by the SecOps Automation Workbench for alert `sample-001`._

## Summary

A High identity alert from `synthetic-identity-provider` involving principal `alice@example.invalid` on asset `idp-tenant-demo`. Identity risk tier is low and asset criticality is high. Recommended playbook: `identity-mfa-fatigue-triage`.

## Observables

- `failed_login_burst` — suspicious: Bursts of failed logins can indicate brute force or password spray. (synthetic)
- `mfa_push_spam` — suspicious: Repeated MFA prompts may indicate MFA fatigue or push bombing. (synthetic)
- `new_country` — suspicious: A first-seen country can indicate account takeover or legitimate travel. (synthetic)
- `successful_login_after_failures` — suspicious: A success after many failures can indicate a guessed or sprayed credential. (synthetic)

## Mapped techniques

- T1078 — Valid Accounts
- T1110 — Brute Force
- T1621 — Multi-Factor Authentication Request Generation

## Recommended playbook

`identity-mfa-fatigue-triage`

## Analyst checklist

- [ ] Confirm this is not known maintenance or an approved test.
- [ ] Validate the principal and asset against your source of truth.
- [ ] Confirm the mapped techniques fit the available evidence.
- [ ] Decide whether to escalate, monitor, or close.
- [ ] Record the decision and rationale in your case-management system.

## Dry-run response plan

All steps below are recommendations only. Nothing is executed automatically.

1. (dry-run) Open a case note and keep every response action in dry-run mode.
2. (dry-run) Ask the user to confirm whether the recent MFA prompts were expected.
3. (dry-run) Review MFA push volume and look for repeated denials followed by a late approval.
4. (dry-run) If MFA fatigue is confirmed, recommend session revocation and password reset as analyst-approved steps.
5. (dry-run) Suggest number-matching or prompt throttling as a follow-up hardening recommendation.

## Limitations

- Generated from synthetic sample data by an engineering workbench, not a production SOC tool.
- Enrichment is synthetic and deterministic; it is not sourced from real identity, asset, or threat-intel systems.
- No response actions were taken; every step is a dry-run recommendation.
- A human analyst must review and own any real-world decision.
